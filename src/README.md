# 25T2_Prototipo_HTML_FacturaAPP
Para poder ejecutar el proyecto, es necesario tener instalado:
1. Instalar el ambiente virtual -> py -m venv .venv
2. Activar el ambiente virtual -> .venv\Scripts\activate
3. Instalar las dependencias -> pip install -r requirements.txt